apiVersion: v1
kind: Service
metadata:
  name: immich-server
  namespace: immich
  labels:
    app: immich-server
    component: server
spec:
  selector:
    app: immich-server
    component: server
  ports:
    - name: http
      port: 2283
      targetPort: 2283
      protocol: TCP
  type: ClusterIP
