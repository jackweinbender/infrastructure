apiVersion: v1
kind: Service
metadata:
  name: twenty
  namespace: twenty
  labels:
    app: twenty
spec:
  selector:
    app: twenty
  ports:
  - name: http
    port: 80
    targetPort: 3000
    protocol: TCP
  type: ClusterIP
