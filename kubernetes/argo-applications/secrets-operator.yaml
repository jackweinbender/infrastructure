apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: k8s-secret-sync
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: k8s-secret-sync
    server: https://kubernetes.default.svc
  project: default
  source:
    path: kubernetes/k8s-secret-sync
    repoURL: git@github.com:jackweinbender/infrastructure.git
    targetRevision: HEAD
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - Validate=true
      - Prune=true
      - PruneLast=true
    automated:
      prune: true
      selfHeal: true
