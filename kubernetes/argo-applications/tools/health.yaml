apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: healthcheck
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: healthcheck
    server: https://kubernetes.default.svc
  project: tools
  source:
    path: kubernetes/manifests/tools/health
    repoURL: git@github.com:jackweinbender/infrastructure.git
    targetRevision: HEAD
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - Validate=true
      - Prune=true
      - PruneLast=true
    automated:
      prune: true
      selfHeal: true
