apiVersion: apps/v1
kind: Deployment
metadata:
  name: immich-server
  namespace: immich
spec:
  replicas: 1
  selector:
    matchLabels:
      app: immich-server
  template:
    metadata:
      labels:
        app: immich-server
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 101000
        runAsGroup: 110000
        fsGroup: 110000
      containers:
        - name: immich-server
          image: ghcr.io/immich-app/immich-server:latest
          ports:
            - containerPort: 2283
              name: http
          envFrom:
            - configMapRef:
                name: immich-config
          env:
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: immich-database-password
                  key: password
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: immich-redis-password
                  key: password
          volumeMounts:
            - name: nas-media-photos
              mountPath: /external-libraries
            - name: nas-media-immich-uploads
              mountPath: /usr/src/app/upload
            - name: localtime
              mountPath: /etc/localtime
              readOnly: true
          livenessProbe:
            httpGet:
              path: /health
              port: 2283
            initialDelaySeconds: 30
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 2283
            initialDelaySeconds: 10
            periodSeconds: 10
      volumes:
        - name: nas-media-photos
          hostPath:
            path: /home/nas/media/photos
            type: DirectoryOrCreate
        - name: nas-media-immich-uploads
          hostPath:
            path: /home/nas/media/immich-uploads
            type: DirectoryOrCreate
        - name: localtime
          hostPath:
            path: /etc/localtime
            type: File
