apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: secrets-operator-binding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: secrets-operator-role
subjects:
  - kind: ServiceAccount
    name: secrets-operator-sa
    namespace: secrets-operator
